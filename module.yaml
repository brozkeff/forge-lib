name: forge-lib
version: 0.5.0
description: "Shared Rust library and CLI binaries for Forge modules — frontmatter parsing, agent deployment, skill installation, module validation. USE WHEN building module tooling, deploying agents, validating conventions."
events: []
